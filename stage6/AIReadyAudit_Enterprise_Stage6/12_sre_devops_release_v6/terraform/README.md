# Terraform Starter

Create environment-specific Terraform modules for DNS, database, object storage, secrets, container hosting, observability and backups. Do not store secrets in state without secure backend controls.
